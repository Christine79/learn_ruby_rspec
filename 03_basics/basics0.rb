def who_is_bigger (a, b, c)
    k = 0
    cle = " "
    aire = [{a: a}, {b: b},{c: c}b, c]
      tab.each do |i|
        puts i
        if i.is_a?
          ]
      aire.each do |h|
        h.each do |i, j|
        puts i
        puts  j.to_s
        if j.is_a?
          if j == nil
              cle = "nil detected"
          end
        elsif j.to_f > k
          cle = "#{i} is bigger"
        end
        k = j
      end
    end
    return cle
end
